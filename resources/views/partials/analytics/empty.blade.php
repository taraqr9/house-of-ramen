{{-- Shown when GA4 answered successfully but the period genuinely has no
     matching events yet (new property, quiet date range) - never treated
     the same as a failed API call above. --}}
<p class="text-muted small mb-0">No analytics data available for this period.</p>
